import AuthComponent from './AuthComponent';
import Container from '../Container';

const AuthContainer = () => {
  // some logic
  return (
    <Container>
      {/* some components */}
      <AuthComponent />
    </Container>
  );
};

export default AuthContainer;
